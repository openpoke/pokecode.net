# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)

local_backend: true

media_folder: 'assets/img'
collections:
  - name: 'members'
    label: 'Members'
    folder: '_members/'
    fields:
      - { name: Title }
